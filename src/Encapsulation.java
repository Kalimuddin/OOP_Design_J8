
/* Encapsulation is the process of combining data and functions into a single unit called class, 
   and restricting direct access from the outside.
   In this, the data is not accessed directly, attributes of the class are kept public, protected, private. 
   getters and setters methods are provided to manipulate these attributes.
   Thus, encapsulation makes the concept of data hiding.
*/

/* Real life eg : ATM Machine: An ATM machine encapsulates the functions related to banking operations, 
   and users can only interact with it through a defined interface (like buttons and screen), hiding the internal processes.
*/

public class Encapsulation {
	public static void main(String args[]) {
		System.out.println("Use same concept of Access Modifiers");
	}
}
